Sock_Stream uses TCP
SOCK_DGRAM USES UDP

Priviileged ports are ports that normal users aren't allowed to run servers on. They are TCP/IP port numbers below 1024.

Doc: https://realpython.com/python-sockets/#using-hostnames